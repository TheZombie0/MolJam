# moljam

Molecular database quality scoring and visualization toolkit.

## Install (dev)

```bash
pip install -e .
```

Notes:
- This project requires RDKit for scoring. Install it via conda (recommended) or your environment's package manager.
- Visualization features require extra dependencies: `pip install -e ".[viz]"`.

